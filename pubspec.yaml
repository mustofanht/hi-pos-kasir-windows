name: jaya_propertiy
description: A new Flutter project.
publish_to: "none"
version: 1.0.0

environment:
  sdk: ">=3.0.6 <4.0.0"

dependencies:
  either_dart: ^1.0.0
  email_validator: ^2.1.17
  fl_chart: ^0.65.0
  floating_bottom_bar: ^0.0.2
  flutter:
    sdk: flutter
  
  flutter_localizations:
    sdk: flutter

  get: ^4.7.2
  get_storage: ^2.1.1
  google_fonts: ^6.1.0
  introduction_screen: ^4.0.0
  jwt_decoder: ^2.0.1
  loading_animation_widget: ^1.2.0+4
  logger: ^2.0.2+1
  # presentation_displays: ^1.0.0
  syncfusion_flutter_charts: ^29.1.37
  table_calendar: ^3.0.9
  flutter_svg: ^2.0.10+1
  flutter_carousel_widget: ^3.1.0
  path_provider: ^2.1.2
  image_picker: ^1.0.7
  # flutter_local_notifications: ^16.0.0
  # print_bluetooth_thermal: ^1.1.0
  esc_pos_utils_plus: ^2.0.2
  thermal_printer: ^1.0.5
  # flutter_usb_printer: ^0.1.0+1
  # thermal_printer: ^1.0.5
  # flutter_thermal_printer: ^0.0.10
  presentation_displays:
    path: ./plugin/presentation_displays
  table_sticky_headers: ^2.0.5
  device_info_plus: ^9.1.2
  auto_size_text: ^3.0.0
  qr_flutter: ^4.1.0

# win32 5.0.9 (pulled in transitively) uses UnmodifiableUint8ListView, which was
# removed in Dart 3.4+. Force a version compatible with the current Dart SDK.
dependency_overrides:
  win32: 5.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/svg/
    - assets/icons/
    - assets/icons/svg/
    # - assets/translations/